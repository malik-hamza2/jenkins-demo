pipeline {
    agent { label 'new-agent' }

    stages {
        stage('Test Agent') {
            steps {
                sh 'echo Running on new Jenkins Agent'
                sh 'hostname'
            }
        }
    }
}
